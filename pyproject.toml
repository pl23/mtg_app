[tool.poetry]
name = "mtg-app"
version = "0.1.0"
description = ""
authors = ["pappy"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
flask = "^3.0.1"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
